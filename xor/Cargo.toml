[package]
name = "xor"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = ["trace", "dump", "lox_errors"]
trace = []
dump = []
lox_errors = []

[dependencies]
num_enum = "0.5.7"
fnv = "1.0.7"
